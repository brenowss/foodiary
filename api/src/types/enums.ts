export enum Goal {
  LOSE = 'lose',
  GAIN = 'gain',
  MAINTAIN = 'maintain',
}

export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
}

export enum MealInputType {
  AUDIO = 'audio',
  PICTURE = 'picture',
}

export enum MealStatus {
  UPLOADING = 'uploading',
  PROCESSING = 'processing',
  SUCCESS = 'success',
  FAILED = 'failed',
}
